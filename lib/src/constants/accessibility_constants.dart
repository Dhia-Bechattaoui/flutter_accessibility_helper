/// Constants for accessibility features
class AccessibilityConstants {
  AccessibilityConstants._();

  /// Minimum contrast ratio for text
  static const double minContrastRatio = 4.5;

  /// Default accessibility label
  static const String defaultLabel = 'Accessible element';

  /// Default accessibility hint
  static const String defaultHint = 'Double tap to activate';

  /// Focus timeout in milliseconds
  static const int focusTimeoutMs = 500;

  /// Announcement delay in milliseconds
  static const int announcementDelayMs = 100;
}
